<h1>Edit Lead</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.supervisor_lead_path(@conn, :update, @lead)) %>

<span><%= link "Back", to: Routes.supervisor_lead_path(@conn, :index) %></span>
